<td><%= post1.title %></td>


<%# 「liked_by?(post.id)」は、ユーザーが「いいね」を押しているかどうかを判別するためのメソッド %>

<% if current_user.liked_by?(post1.id) %>    
  <td><%= link_to 'いいね外す１', destroy_like_path(post1), method: :DELETE, remote: true %> <%= post1.likes.count %></td>
<% else %>
  <td><%= link_to 'いいね付ける１', create_like_path(post1), method: :POST, remote: true %> <%= post1.likes.count %></td>
<% end %>




